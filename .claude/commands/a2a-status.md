Show the current A2A system status. Run this command:

```bash
python ".a2a/toggle.py"
```

Then read `.a2a/status.json` and report: current mode (ON/OFF), phase, current task, and completed tasks count.
